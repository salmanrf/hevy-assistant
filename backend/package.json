{
  "name": "backend",
  "version": "0.1.0",
  "scripts": {
    "initiate": "python3 -m venv venv && . venv/bin/activate && pip install -r requirements.txt",
    "dev": ". venv/bin/activate && python main.py --env dev"
  }
}
